# SGC SmartTrack — Vercel prototype

Static login / register / role portals for DepEd SDO Cadiz City.

## Deploy

From this folder:

```bash
npx vercel --yes
npx vercel --prod --yes
```

Or in Vercel dashboard: Import this directory as the **Root Directory** (`docs/ui-prototype`).
